# Personal academic website

Template credits to https://github.com/sbryngelson/academic-website-template


## Build

Run `$ bundle exec jekyll serve --port 9000` in the repository root directory to build and run the site locally.

